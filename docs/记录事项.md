  2. memory_writer.py prompt 没教 AI 识别纠正意图 → 纠正消息被当新事实抽取成 ADD
  3. FeedbackHandler 需手动调用 → 没有自动纠正检测

  需要修复：

  1. 启用 versioning（config.yaml）
  2. 改进 memory_writer.py prompt，让 AI 能区分"新事实"和"纠正旧事实"
  3. 在 memory_updater.py 添加自动纠正检测
  
  
  已完成 ✅

  ┌────────┬──────────────────────────────────────────────────────────┬───────────┐
  │ Phase  │                           内容                           │   状态    │
  ├────────┼──────────────────────────────────────────────────────────┼───────────┤
  │ Phase  │ 版本支持（SemanticMemoryRecord + SemanticMemoryStore +   │ ✅        │
  │ 6      │ FeedbackHandler）                                        │ 测试通过  │
  ├────────┼──────────────────────────────────────────────────────────┼───────────┤
  │ Phase  │ Quality Gate + Write Verifier +                          │ ✅        │
  │ 5      │ MemoryWriter/MemoryUpdater 集成                          │ 测试通过  │
  ├────────┼──────────────────────────────────────────────────────────┼───────────┤
  │ Phase  │ Memory Graph（GraphStore + RetrievalRouter 集成）        │ ✅        │
  │ 7      │                                                          │ 测试通过  │
  ├────────┼──────────────────────────────────────────────────────────┼───────────┤
  │ Phase  │ Lifecycle Manager（decay + merge + janitor）+            │ ✅        │
  │ 8      │ MemoryManager 集成                                       │ 测试通过  │
  └────────┴──────────────────────────────────────────────────────────┴───────────┘

  未完成 ⚠️

  1. Phase 7.2 - MemoryWriter.extract_relations()
  方法未实现（从对话中抽取实体关系三元组存入图）
  2. 手动验证 - 计划中每个 Phase 的验证方法（6.6, 5.5, 7.5, 8.7）未执行
  3. Git tags - phase5-complete ~ phase8-complete 未打标签

  需要我补充 extract_relations 方法吗？还是先运行评测系统验证整体效果？
